Sticky site header: 64px, translucent black + blur, one primary CTA, max 7 nav items (more goes in a submenu or the footer).

```jsx
<Header items={[{label:'Výsledky',href:'/vysledky'},{label:'Pro diváky',href:'/divaci'}]} activeHref="/vysledky" />
```
